//Don't change this file manually, it was generated by automation tool.

#pragma once

#include <memory>
#include <string>
#include <vector>
#include <map>

namespace FDN <%

void initialize();

struct Logger {

    static std::shared_ptr<Logger> infoHandler();
    static std::shared_ptr<Logger> errorHandler();

    virtual void print(const std::string &text) = 0;

    virtual ~Logger() {}
};

%> //namespace FDN
